{% extends "radpress/base.html" %}

{% load blocktrans trans from i18n %}
{% load url from future %}
{% load thumbnail %}
{% load radpress_datetime radpress_full_name from radpress_tags %}

{% block title %}{% trans "Mainpage" %}{% endblock %}

{% block content %}
  {% for object in object_list %}
    <div class="post-wrap">
      <div class="post-5 post type-post status-publish format-gallery hentry category-uncategorized post">
        <div class="box">
          {% if object.cover_image %}
            <div class="gallery-wrap">
              <a href="{{ object.get_absolute_url }}" title="{{ object.title }}">
                <img src="{{ object.cover_image.image.url }}" alt="{{ object.cover_image.name }}" />
              </a>
            </div>
            <div class="clear"></div>
          {% endif %}
          <div class="frame">
            <div class="title-wrap">
              <h2 class="entry-title">
                <a href="{{ object.get_absolute_url }}" title="{{ object.title }}">
                  {{ object.title }}
                </a>
              </h2>
              <div class="title-meta">
                <span>{{ object.author|radpress_full_name }}</span>
                <span>{% radpress_datetime object.updated_at %}</span>
              </div>
            </div>
            <div class="post-content">
              {{ object.content_by_more|safe }}
              <p>
                <a class="more-link" href="{{ object.get_absolute_url }}">{% trans "Read more" %} →</a>
              </p>
            </div>
            <div class="bar">
              <div class="bar-frame clearfix">
                <div class="meta-info">
                  <div class="share">
                    {% include "radpress/partials/share.html" %}
                  </div>
                  {% if object.tags.count %}
                    <div class="tags">
                      <div class="title entypo">i</div>

                      {% for tag in object.tags.all %}
                        <a rel="tag" title="{% blocktrans %}View all posts in {{ tag.name }}{% endblocktrans %}" href="{% url 'radpress-article-archive' %}?tag={{ tag.slug }}">
                          {{ tag.name }}
                        </a>
                      {% endfor %}
                    </div>
                  {% endif %}
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  {% empty %}
    <p>{% trans "There's no post for now." %}</p>
  {% endfor %}
{% endblock %}
