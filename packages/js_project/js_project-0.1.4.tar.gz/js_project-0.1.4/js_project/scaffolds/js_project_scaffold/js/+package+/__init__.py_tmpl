from fanstatic import Library, Resource, Group

library = Library('{{package}}', 'resources')

{{package}}_js = Resource(library, '{{package}}.js')

{{package}}_css = Resource(library, '{{package}}.css')

{{package}} = Group([{{package}}_js, {{package}}_css])
