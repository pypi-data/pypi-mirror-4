#!/usr/bin/env python
import os
import sys

if os.environ.get('KIDE_PATH', None) is not None:
    sys.path.insert(0, os.environ.get('KIDE_PATH'))

from kide import kide

if __name__ == '__main__':
    kide.main()
