from distutils.core import setup

setup(
    name='test_pr',
    version='0.0.1',
    py_modules=['test_pr'],
    author='Xzeroo Chan',
    description='It is just a test,do not use it!',
    )
