#!/usr/bin/env python

import json
import os


if __name__ == '__main__':
    if os.path.isfile("setup.py"):
        print 20
    else:
        print json.dumps({'score': 0, 'reason': "Couldn't find a setup.py"})
