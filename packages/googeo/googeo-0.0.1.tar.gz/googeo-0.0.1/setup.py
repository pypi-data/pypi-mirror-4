from setuptools import find_packages, setup

setup(

    name='googeo',

    version='0.0.1',

    modules=['googeo'],

    # packages=find_packages('src'),

    # include_package_data=True,

    # package_dir={'': 'src'},

    install_requires=[
    ],

    scripts=[]
)
