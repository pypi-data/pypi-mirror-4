define(ARCH, ifelse(BITS, 64, x86_64, i386))dnl
define(HOSTNAME, `fedora'VERSION)dnl
define(ROOTPW, ravelloCloud)dnl
define(FSTYPE, ext4)dnl
define(PY_VERSION, 2.7)dnl
define(PY3_VERSION, ifelse(VERSION, 17, 3.2, 3.3))dnl
